# Default values for backend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

service:
  name: api-service
  type: LoadBalancer
  externalPort: 5000
  internalPort: 5000
configmap:
  name: api-configmap
deployment:
  name: api-deployment
  app: api
image:
  repository: gcr.io/phono-ai/api
  tag: latest
  pullPolicy: Always
resources: {}
